class SnippetManager:
    def GetMinAndMaxSnippetLength(self, snippets):
        min_len = 100
        max_len = 0
        # ... inner logic to calculate lengths (can assume it's correct)
        return (min_len, max_len)